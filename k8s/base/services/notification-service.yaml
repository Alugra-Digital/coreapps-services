apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: coreapps
  labels:
    app: notification-service
    component: microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        component: microservice
    spec:
      containers:
        - name: notification-service
          image: coreapps-notification-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3011
              name: http
            - containerPort: 3011
              name: ws
              protocol: TCP
          env:
            - name: NODE_ENV
              value: "production"
          envFrom:
            - secretRef:
                name: coreapps-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3011
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3011
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: coreapps
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
    - port: 3011
      targetPort: 3011
      name: http
  selector:
    app: notification-service

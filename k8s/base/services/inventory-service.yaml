apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: coreapps
  labels:
    app: inventory-service
    component: microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
        component: microservice
    spec:
      containers:
        - name: inventory-service
          image: coreapps-inventory-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3005
              name: http
          env:
            - name: NODE_ENV
              value: "production"
          envFrom:
            - secretRef:
                name: coreapps-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: coreapps
  labels:
    app: inventory-service
spec:
  type: ClusterIP
  ports:
    - port: 3005
      targetPort: 3005
      name: http
  selector:
    app: inventory-service

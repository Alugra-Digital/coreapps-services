apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounting-service
  namespace: coreapps
  labels:
    app: accounting-service
    component: microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: accounting-service
  template:
    metadata:
      labels:
        app: accounting-service
        component: microservice
    spec:
      containers:
        - name: accounting-service
          image: coreapps-accounting-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3006
              name: http
          env:
            - name: NODE_ENV
              value: "production"
          envFrom:
            - secretRef:
                name: coreapps-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: accounting-service
  namespace: coreapps
  labels:
    app: accounting-service
spec:
  type: ClusterIP
  ports:
    - port: 3006
      targetPort: 3006
      name: http
  selector:
    app: accounting-service
